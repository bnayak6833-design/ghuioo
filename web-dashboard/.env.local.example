# Example environment variables for local development or Vercel

# Coordinator base URL.  When running locally this is the dev server,
# but in production point it at your deployed coordinator service.
NEXT_PUBLIC_COORDINATOR_URL=http://localhost:8000
